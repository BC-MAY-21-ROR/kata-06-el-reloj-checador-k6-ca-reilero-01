<!DOCTYPE html>
<html>
  <head>
    <title>RelojChecador</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="body">
    
    <header>
      <nav class="navbar ">
        <h1 class="watch-title" >Reloj checador</h1>
        
        <% if signed_in? %>
        <%=button_to 'Cerrar Sesión', destroy_user_session_path, class:'button_header', method: :delete, form: {data: {turbo_confirm: 'Desea cerrar sesión?'}} %>
        <%else%>
        <%=button_to 'Login',new_user_session_path, method: :get, class: 'button_header button_login'%>
        <%=button_to 'Registrarse',new_user_registration_path,method: :get, class: 'button_header'%> 
        <% end %>
      </nav>
    </header>
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>
    <%= yield %>
    
  </body>
</html>
